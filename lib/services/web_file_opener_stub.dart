/// Stub — web dışı platformlar için
void openFileInBrowser(String url, {String? fileName}) {
  // no-op on non-web
}

void downloadFileInBrowser(String url, {String? fileName}) {
  // no-op on non-web
}
